import styled from 'styled-components'

export const Wrapper = styled.div`
  .handle {
    display: flex;
    margin: 8px 0 16px;
    position: relative;
    .search-name {
      width: 300px;
    }

    .category-btn {
      position: absolute;
      right: 30px;
    }
  }
`
